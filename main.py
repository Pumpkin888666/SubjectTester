# import time
# from alive_progress import alive_bar
#
# with alive_bar(title="请求大模型中",spinner="stars",bar=None,spinner_length=30,elapsed="{elapsed}",monitor=False,stats=False) as bar:   # 不传 total
#     time.sleep(5)
# # 只看到 spinner 在转 + 计数
